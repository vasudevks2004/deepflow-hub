services:
  - type: web
    name: deepflow-mastery-hub
    env: static
    runtime: static
    buildCommand: npm install && npm run build
    publishDir: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

services:
  - type: web
    name: deepflow-mastery-hub
    env: static
    runtime: static # Added for clarity
    buildCommand: npm install && npm run build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /*

        destination: /index.html
